import { Inter } from "next/font/google";

const interTitle = Inter({subsets:["latin"], weight:"500"});

const interSecondary = Inter({subsets:["latin"], weight:"400" });

export {interTitle, interSecondary}